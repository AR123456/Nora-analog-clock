function App() {
  return (
    <div>
      <h2>Let's get started!</h2>
      <p>This is also visable! </p>
      {/* If this was just JS would look like this :
      const para = document.createElement("p");
      para.textContent = "This is also visable"
      document.getElementById("root").append(para)
      
      */}
    </div>
  );
}

export default App;
